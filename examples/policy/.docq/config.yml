llm:
  enabled: true
  # base_url и model можно менять без кода:
  base_url: https://api.openai.com/v1
  model: gpt-4o-mini
  timeout_s: 60
  temperature: 0.0
  max_tokens: 1200

  # optional allow-list (если хотите запретить случайные модели)
  # allowed_models: ["gpt-4o-mini", "gpt-4.1-mini"]
